package klt.mdy.kotlinserializationtest.common

object Endpoints {
    private const val HOST = "https://quotes15.p.rapidapi.com"
    private const val PATH = "/quotes"
    private const val RANDOM = "/random/"

    const val RANDOM_QUOTES = HOST + PATH + RANDOM
}